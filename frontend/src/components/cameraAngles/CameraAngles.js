import React, { Component } from 'react'
import PropTypes from 'prop-types'

import classes from './CameraAngles.module.css'

class CameraAngles extends Component {
    static propTypes = {
    }

    render() {
        return (
            <div>
                CameraAngles
            </div>
        )
    }
}

export default CameraAngles